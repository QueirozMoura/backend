# Banco de Dados
DB_USER=postgres
DB_PASSWORD=postgre
DB_HOST=localhost
DB_PORT=5432
DB_NAME=assistencia_tecnica

# Servidor
PORT=3000
NODE_ENV=development

# JWT — ALTERE PARA UMA STRING SECRETA LONGA EM PRODUÇÃO
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=mude_este_segredo_em_producao_use_openssl_rand_base64_64
